@model WestendMotors.Models.TradeInRequest

@{
    ViewBag.Title = "Trade-In Request Details";
    var role = Session["Role"] as string;

    if (role == "Admin")
    {
        Layout = "~/Views/Shared/_Layout.cshtml";
    }
    else
    {
        Layout = "~/Views/Shared/_Layout_User.cshtml";
    }
}

<div class="container mt-4">
    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success">
            <i class="fas fa-check-circle me-2"></i>@TempData["Success"]
        </div>
    }

    <!-- Page Header -->
    <div class="page-header mb-4">
        <div class="d-flex align-items-center">
            <div class="header-icon">
                <i class="fas fa-exchange-alt"></i>
            </div>
            <div>
                <h1 class="mb-1">Trade-In Request Details</h1>
                <p class="text-muted mb-0">Request #@Model.TradeInRequestId - @Model.Customer.FullName</p>
            </div>
        </div>
        <div class="header-actions">
            <span class="status-badge status-@Model.Status.ToLower().Replace(" ", "-")">
                @Model.Status
            </span>
        </div>
    </div>

    <!-- Main Content Row -->
    <div class="row">
        <!-- Left Column: Vehicle Details -->
        <div class="col-lg-8 mb-4">
            <!-- Admin Review Button (Visible only to Admins) -->
            @if (role == "Admin")
            {
                <div class="admin-actions-card mb-4">
                    <div class="card-body">
                        <h6 class="card-title">
                            <i class="fas fa-user-cog me-2"></i>Admin Actions
                        </h6>
                        <div class="d-flex gap-2">
                            @Html.ActionLink("Detailed Review", "AdminReview", new { id = Model.TradeInRequestId },
                                new { @class = "btn btn-warning" })
                            @Html.ActionLink("Edit Request", "Edit", new { id = Model.TradeInRequestId },
                                new { @class = "btn btn-info" })
                        </div>
                        <small class="text-muted">Access full detailed review with images and appointment scheduling</small>
                    </div>
                </div>
            }

            <!-- Vehicle Information Card -->
            <div class="details-card mb-4">
                <div class="card-header">
                    <h5><i class="fas fa-car me-2"></i>Vehicle Information</h5>
                </div>
                <div class="card-body">
                    <div class="row">
                        <div class="col-md-6">
                            <div class="detail-group">
                                <label class="detail-label">
                                    <i class="fas fa-industry me-2"></i>Make
                                </label>
                                <div class="detail-value">@Model.Make</div>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="detail-group">
                                <label class="detail-label">
                                    <i class="fas fa-tag me-2"></i>Model
                                </label>
                                <div class="detail-value">@Model.Model</div>
                            </div>
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-md-6">
                            <div class="detail-group">
                                <label class="detail-label">
                                    <i class="fas fa-calendar-alt me-2"></i>Year
                                </label>
                                <div class="detail-value">@Model.Year</div>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="detail-group">
                                <label class="detail-label">
                                    <i class="fas fa-tachometer-alt me-2"></i>Mileage
                                </label>
                                <div class="detail-value">@Model.Mileage.ToString("N0") km</div>
                            </div>
                        </div>
                    </div>

                    @if (!string.IsNullOrEmpty(Model.FuelType))
                    {
                        <div class="row">
                            <div class="col-md-6">
                                <div class="detail-group">
                                    <label class="detail-label">
                                        <i class="fas fa-gas-pump me-2"></i>Fuel Type
                                    </label>
                                    <div class="detail-value">@Model.FuelType</div>
                                </div>
                            </div>
                            @if (!string.IsNullOrEmpty(Model.Transmission))
                            {
                                <div class="col-md-6">
                                    <div class="detail-group">
                                        <label class="detail-label">
                                            <i class="fas fa-cogs me-2"></i>Transmission
                                        </label>
                                        <div class="detail-value">@Model.Transmission</div>
                                    </div>
                                </div>
                            }
                        </div>
                    }

                    @if (Model.EstimatedValue.  HasValue)
                    {
                        <div class="detail-group">
                            <label class="detail-label">
                                <i class="fas fa-money-bill-wave me-2"></i>Customer Estimated Value
                            </label>
                            <div class="detail-value highlight-value">@Model.EstimatedValue.Value.ToString("C")</div>
                        </div>
                    }

                    @if (!string.IsNullOrEmpty(Model.ConditionNotes))
                    {
                        <div class="detail-group">
                            <label class="detail-label">
                                <i class="fas fa-clipboard me-2"></i>Condition Notes
                            </label>
                            <div class="detail-value">@Model.ConditionNotes</div>
                        </div>
                    }
                </div>
            </div>

            <!-- Customer Images Section -->
            @if (Model.Images != null && Model.Images.Any())
            {
                <div class="images-card">
                    <div class="card-header">
                        <h5><i class="fas fa-camera me-2"></i>Vehicle Images (@Model.Images.Count())</h5>
                    </div>
                    <div class="card-body">
                        <div class="image-gallery">
                            @foreach (var image in Model.Images)
                            {
                                <div class="gallery-item">
                                    <img src="@image.ImagePath" class="gallery-image" alt="Vehicle Image" />
                                    <div class="gallery-overlay">
                                        <a href="@image.ImagePath" target="_blank" class="btn btn-light btn-sm">
                                            <i class="fas fa-expand me-1"></i>View Full
                                        </a>
                                    </div>
                                </div>
                            }
                        </div>
                    </div>
                </div>
            }
        </div>

        <!-- Right Column: Status & Actions -->
        <div class="col-lg-4 mb-4">
            <!-- Request Summary Card -->
            <div class="summary-card mb-4">
                <div class="card-header">
                    <h6><i class="fas fa-info-circle me-2"></i>Request Summary</h6>
                </div>
                <div class="card-body">
                    <div class="summary-item">
                        <div class="summary-label">Request Date</div>
                        <div class="summary-value">@Model.RequestDate.ToString("MMM dd, yyyy")</div>
                    </div>

                    @if (Model.ScheduledAppointment.HasValue)
                    {
                        <div class="summary-item">
                            <div class="summary-label">Scheduled Appointment</div>
                            <div class="summary-value appointment-highlight">
                                <i class="fas fa-calendar-check me-1"></i>
                                @Model.ScheduledAppointment.Value.ToString("MMM dd, yyyy")
                                <br>
                                <small>@Model.ScheduledAppointment.Value.ToString("h:mm tt")</small>
                            </div>
                        </div>
                    }

                    @if (Model.FinalOffer.HasValue)
                    {
                        <div class="summary-item">
                            <div class="summary-label">Final Offer</div>
                            <div class="summary-value offer-highlight">
                                @Model.FinalOffer.Value.ToString("C")
                            </div>
                        </div>
                    }

                    @if (Model.AdminReviewDate.HasValue)
                    {
                        <div class="summary-item">
                            <div class="summary-label">Last Reviewed</div>
                            <div class="summary-value">@Model.AdminReviewDate.Value.ToString("MMM dd, yyyy")</div>
                        </div>
                    }
                </div>
            </div>

            <!-- Admin Notes -->
            @if (!string.IsNullOrEmpty(Model.AdminNotes) || role == "Admin")
            {
                <div class="notes-card mb-4">
                    <div class="card-header">
                        <h6><i class="fas fa-sticky-note me-2"></i>Admin Notes</h6>
                    </div>
                    <div class="card-body">
                        @if (!string.IsNullOrEmpty(Model.AdminNotes))
                        {
                            <p class="notes-content">@Model.AdminNotes</p>
                        }
                        else
                        {
                            <p class="text-muted">No admin notes yet</p>
                        }
                    </div>
                </div>
            }

            <!-- Status Update Form (Admin only) -->
            @if (role == "Admin")
            {
                <div class="status-update-card">
                    <div class="card-header">
                        <h6><i class="fas fa-edit me-2"></i>Update Status</h6>
                    </div>
                    <div class="card-body">
                        @using (Html.BeginForm("UpdateStatus", "TradeInRequests", FormMethod.Post))
                        {
                            @Html.AntiForgeryToken()
                            @Html.Hidden("id", Model.TradeInRequestId)

                            <div class="form-group mb-3">
                                <label class="form-label">
                                    <i class="fas fa-flag me-2"></i>Status
                                </label>
                                <select name="status" class="form-control">
                                    <option value="Pending" @(Model.Status == "Pending" ? "selected" : "")>Pending</option>
                                    <option value="Under Review" @(Model.Status == "Under Review" ? "selected" : "")>Under Review</option>
                                    <option value="Approved" @(Model.Status == "Approved" ? "selected" : "")>Approved</option>
                                    <option value="Declined" @(Model.Status == "Declined" ? "selected" : "")>Declined</option>
                                    <option value="Scheduled" @(Model.Status == "Scheduled" ? "selected" : "")>Scheduled</option>
                                </select>
                            </div>

                            <div class="form-group mb-3 appointment-field">
                                <label class="form-label">
                                    <i class="fas fa-calendar-alt me-2"></i>Appointment Date
                                </label>
                                <input type="datetime-local" name="newDate"
                                       value="@(Model.ScheduledAppointment?.ToString("yyyy-MM-ddTHH:mm"))"
                                       class="form-control" />
                            </div>

                            <div class="form-group mb-3">
                                <label class="form-label">
                                    <i class="fas fa-comment me-2"></i>Notes
                                </label>
                                <textarea name="notes" placeholder="Add notes here..."
                                          class="form-control" rows="3">@Model.AdminNotes</textarea>
                            </div>

                            <button type="submit" class="btn btn-success w-100">
                                <i class="fas fa-save me-2"></i>Update Status
                            </button>
                        }
                    </div>
                </div>
            }

            <!-- Customer Progress (Customer view) -->
            @if (role == "Customer")
            {
                <div class="progress-card">
                    <div class="card-header">
                        <h6><i class="fas fa-chart-line me-2"></i>Your Progress</h6>
                    </div>
                    <div class="card-body">
                        <div class="progress-steps">
                            <div class="progress-step @(new[] { "Pending", "Under Review", "Approved", "Declined", "Scheduled" }.Contains(Model.Status) ? "active" : "")">
                                <div class="step-icon">
                                    <i class="fas fa-paper-plane"></i>
                                </div>
                                <div class="step-content">
                                    <div class="step-title">Request Submitted</div>
                                    <div class="step-date">@Model.RequestDate.ToString("MMM dd")</div>
                                </div>
                            </div>

                            <div class="progress-step @(new[] { "Under Review", "Approved", "Declined", "Scheduled" }.Contains(Model.Status) ? "active" : "")">
                                <div class="step-icon">
                                    <i class="fas fa-search"></i>
                                </div>
                                <div class="step-content">
                                    <div class="step-title">Under Review</div>
                                    @if (Model.AdminReviewDate.HasValue)
                                    {
                                        <div class="step-date">@Model.AdminReviewDate.Value.ToString("MMM dd")</div>
                                    }
                                </div>
                            </div>

                            @if (Model.Status == "Scheduled" || Model.ScheduledAppointment.HasValue)
                            {
                                <div class="progress-step active">
                                    <div class="step-icon">
                                        <i class="fas fa-calendar-check"></i>
                                    </div>
                                    <div class="step-content">
                                        <div class="step-title">Appointment Scheduled</div>
                                        @if (Model.ScheduledAppointment.HasValue)
                                        {
                                            <div class="step-date">@Model.ScheduledAppointment.Value.ToString("MMM dd")</div>
                                        }
                                    </div>
                                </div>
                            }

                            @if (Model.Status == "Approved")
                            {
                                <div class="progress-step active completed">
                                    <div class="step-icon">
                                        <i class="fas fa-check-circle"></i>
                                    </div>
                                    <div class="step-content">
                                        <div class="step-title">Approved</div>
                                        @if (Model.FinalOffer.HasValue)
                                        {
                                            <div class="step-offer">@Model.FinalOffer.Value.ToString("C")</div>
                                        }
                                    </div>
                                </div>
                            }
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>

    <!-- Back Button -->
    <div class="action-buttons">
        @Html.ActionLink("Back to List", "Index", null, new { @class = "btn btn-outline-secondary" })
    </div>
</div>

@section Scripts {
    <script>
        // Show/hide appointment date based on status selection
        $(document).ready(function () {
            $('select[name="status"]').change(function () {
                var appointmentField = $('.appointment-field');
                if ($(this).val() === 'Scheduled') {
                    appointmentField.show();
                } else {
                    appointmentField.hide();
                }
            }).trigger('change');
        });

        // Image gallery hover effects
        $('.gallery-item').hover(function () {
            $(this).find('.gallery-overlay').fadeIn(200);
        }, function () {
            $(this).find('.gallery-overlay').fadeOut(200);
        });
    </script>
}

@section Styles {
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet">
    <style>
        /* Matching Dashboard Theme - Clean Automotive Design */
        :root {
            --primary-color: #2c3e50;
            --secondary-color: #34495e;
            --accent-color: #3498db;
            --success-color: #27ae60;
            --warning-color: #f39c12;
            --danger-color: #e74c3c;
            --light-bg: #f8f9fa;
            --card-bg: #ffffff;
            --text-dark: #2c3e50;
            --text-muted: #7f8c8d;
            --border-color: #e1e8ed;
            --shadow: 0 2px 10px rgba(0,0,0,0.08);
            --shadow-hover: 0 4px 20px rgba(0,0,0,0.12);
        }

        body {
            background-color: var(--light-bg);
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            color: var(--text-dark);
        }

        /* Page Header */
        .page-header {
            background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
            color: white;
            padding: 2rem;
            border-radius: 12px;
            margin-bottom: 2rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .header-icon {
            background: rgba(255,255,255,0.2);
            border-radius: 12px;
            width: 60px;
            height: 60px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-right: 1.5rem;
            font-size: 1.5rem;
        }

        .page-header h1 {
            font-size: 2rem;
            font-weight: 700;
            margin: 0;
        }

        .header-actions {
            display: flex;
            align-items: center;
        }

        /* Status Badges */
        .status-badge {
            padding: 0.5rem 1rem;
            border-radius: 20px;
            font-size: 0.875rem;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .status-pending {
            background: rgba(241, 196, 15, 0.2);
            color: #f1c40f;
            border: 2px solid #f1c40f;
        }

        .status-under-review {
            background: rgba(52, 152, 219, 0.2);
            color: #3498db;
            border: 2px solid #3498db;
        }

        .status-approved {
            background: rgba(39, 174, 96, 0.2);
            color: #27ae60;
            border: 2px solid #27ae60;
        }

        .status-declined {
            background: rgba(231, 76, 60, 0.2);
            color: #e74c3c;
            border: 2px solid #e74c3c;
        }

        .status-scheduled {
            background: rgba(243, 156, 18, 0.2);
            color: #f39c12;
            border: 2px solid #f39c12;
        }

        /* Cards */
        .details-card, .images-card, .summary-card, .notes-card, .status-update-card, .progress-card, .admin-actions-card {
            background: var(--card-bg);
            border-radius: 12px;
            box-shadow: var(--shadow);
            overflow: hidden;
        }

        .card-header {
            background: #f8f9fa;
            padding: 1.25rem 1.5rem;
            border-bottom: 1px solid var(--border-color);
        }

        .card-header h5, .card-header h6 {
            margin: 0;
            font-weight: 600;
            color: var(--text-dark);
            display: flex;
            align-items: center;
        }

        .card-body {
            padding: 1.5rem;
        }

        /* Detail Groups */
        .detail-group {
            margin-bottom: 1.5rem;
        }

        .detail-group:last-child {
            margin-bottom: 0;
        }

        .detail-label {
            font-weight: 600;
            color: var(--text-muted);
            font-size: 0.875rem;
            margin-bottom: 0.5rem;
            display: flex;
            align-items: center;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .detail-value {
            font-size: 1rem;
            font-weight: 600;
            color: var(--text-dark);
            padding: 0.75rem 1rem;
            background: #f8f9fa;
            border-radius: 6px;
            border-left: 4px solid var(--accent-color);
        }

        .highlight-value {
            background: rgba(39, 174, 96, 0.1);
            border-left-color: var(--success-color);
            color: var(--success-color);
            font-size: 1.25rem;
        }

        /* Image Gallery */
        .image-gallery {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
            gap: 1rem;
        }

        .gallery-item {
            position: relative;
            overflow: hidden;
            border-radius: 8px;
            background: #f8f9fa;
        }

        .gallery-image {
            width: 100%;
            height: 150px;
            object-fit: cover;
            transition: transform 0.3s ease;
        }

        .gallery-overlay {
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(0,0,0,0.7);
            display: flex;
            align-items: center;
            justify-content: center;
            opacity: 0;
            transition: opacity 0.3s ease;
        }

        .gallery-item:hover .gallery-overlay {
            opacity: 1;
        }

        .gallery-item:hover .gallery-image {
            transform: scale(1.05);
        }

        /* Summary Items */
        .summary-item {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            padding: 0.75rem 0;
            border-bottom: 1px solid #f1f3f4;
        }

        .summary-item:last-child {
            border-bottom: none;
        }

        .summary-label {
            font-weight: 600;
            color: var(--text-muted);
            font-size: 0.875rem;
        }

        .summary-value {
            font-weight: 600;
            color: var(--text-dark);
            text-align: right;
        }

        .appointment-highlight {
            color: var(--warning-color);
        }

        .offer-highlight {
            color: var(--success-color);
            font-size: 1.25rem;
        }

        /* Notes */
        .notes-content {
            background: #f8f9fa;
            padding: 1rem;
            border-radius: 6px;
            border-left: 4px solid var(--accent-color);
            margin: 0;
        }

        /* Form Styling */
        .form-group {
            margin-bottom: 1.5rem;
        }

        .form-label {
            font-weight: 600;
            margin-bottom: 0.5rem;
            color: var(--text-dark);
            font-size: 0.875rem;
            display: flex;
            align-items: center;
        }

        .form-control {
            border: 2px solid var(--border-color);
            border-radius: 8px;
            padding: 0.75rem 1rem;
            font-size: 0.9rem;
            transition: all 0.2s ease;
            width: 100%;
        }

        .form-control:focus {
            outline: none;
            border-color: var(--accent-color);
            box-shadow: 0 0 0 3px rgba(52, 152, 219, 0.1);
        }

        .form-control:hover {
            border-color: #c1c9d0;
        }

        .appointment-field {
            display: none;
        }

        /* Progress Steps (Customer View) */
        .progress-steps {
            display: flex;
            flex-direction: column;
            gap: 1rem;
        }

        .progress-step {
            display: flex;
            align-items: flex-start;
            padding: 1rem;
            border-radius: 8px;
            background: #f8f9fa;
            opacity: 0.6;
            transition: all 0.3s ease;
        }

        .progress-step.active {
            background: rgba(52, 152, 219, 0.1);
            opacity: 1;
            border-left: 4px solid var(--accent-color);
        }

        .progress-step.completed {
            background: rgba(39, 174, 96, 0.1);
            border-left-color: var(--success-color);
        }

        .step-icon {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            background: var(--text-muted);
            color: white;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-right: 1rem;
            flex-shrink: 0;
        }

        .progress-step.active .step-icon {
            background: var(--accent-color);
        }

        .progress-step.completed .step-icon {
            background: var(--success-color);
        }

        .step-content {
            flex: 1;
        }

        .step-title {
            font-weight: 600;
            color: var(--text-dark);
            margin-bottom: 0.25rem;
        }

        .step-date {
            font-size: 0.8rem;
            color: var(--text-muted);
        }

        .step-offer {
            font-size: 1.1rem;
            font-weight: 700;
            color: var(--success-color);
        }

        /* Alert Styling */
        .alert {
            border: none;
            border-radius: 8px;
            padding: 1rem 1.25rem;
            margin-bottom: 2rem;
            font-weight: 500;
            display: flex;
            align-items: center;
        }

        .alert-success {
            background: #d4edda;
            color: #155724;
            border-left: 4px solid var(--success-color);
        }

        /* Button Styling */
        .btn {
            font-weight: 600;
            padding: 0.75rem 1.5rem;
            border-radius: 8px;
            transition: all 0.2s ease;
            border: none;
            cursor: pointer;
            font-size: 0.9rem;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            justify-content: center;
        }

        .btn-success {
            background: var(--success-color);
            color: white;
        }

        .btn-success:hover {
            background: #219a52;
            transform: translateY(-2px);
            box-shadow: var(--shadow-hover);
            color: white;
            text-decoration: none;
        }

        .btn-warning {
            background: var(--warning-color);
            color: white;
        }

        .btn-warning:hover {
            background: #e67e22;
            transform: translateY(-2px);
            box-shadow: var(--shadow-hover);
            color: white;
            text-decoration: none;
        }

        .btn-info {
            background: var(--accent-color);
            color: white;
        }

        .btn-info:hover {
            background: #2980b9;
            transform: translateY(-2px);
            box-shadow: var(--shadow-hover);
            color: white;
            text-decoration: none;
        }

        .btn-outline-secondary {
            background: transparent;
            color: var(--text-muted);
            border: 2px solid var(--border-color);
        }

        .btn-outline-secondary:hover {
            background: var(--secondary-color);
            border-color: var(--secondary-color);
            color: white;
            text-decoration: none;
            transform: translateY(-2px);
        }

        .btn-light {
            background: rgba(255,255,255,0.9);
            color: var(--text-dark);
            border: none;
        }

        .btn-light:hover {
            background: white;
            color: var(--text-dark);
            text-decoration: none;
        }

        .btn-sm {
            padding: 0.5rem 1rem;
            font-size: 0.8rem;
        }

        .w-100 {
            width: 100%;
        }

        /* Action Buttons */
        .action-buttons {
            margin-top: 2rem;
            padding-top: 2rem;
            border-top: 1px solid var(--border-color);
        }

        /* Admin Actions Card */
        .admin-actions-card .card-body {
            padding: 1.25rem 1.5rem;
        }

        .admin-actions-card .card-title {
            font-size: 1rem;
            margin-bottom: 1rem;
        }

        /* Responsive Design */
        @@media (max-width: 768px) {
            .page-header {
                padding: 1.5rem;
                flex-direction: column;
                align-items: flex-start;
                gap: 1rem;
            }

            .header-icon {
                margin-right: 1rem;
            }

            .image-gallery {
                grid-template-columns: repeat(auto-fill, minmax(150px, 1fr));
                gap: 0.75rem;
            }

            .gallery-image {
                height: 120px;
            }

            .summary-item {
                flex-direction: column;
                align-items: flex-start;
                gap: 0.5rem;
            }

            .summary-value {
                text-align: left;
            }

            .card-body {
                padding: 1.25rem;
            }

            .detail-value {
                padding: 0.5rem 0.75rem;
                font-size: 0.9rem;
            }

            .btn {
                width: 100%;
                justify-content: center;
                margin-bottom: 0.5rem;
            }

            .admin-actions-card .d-flex {
                flex-direction: column;
                gap: 0.75rem;
            }
        }

        @@media (max-width: 576px) {
            .page-header {
                padding: 1.25rem;
            }

            .page-header h1 {
                font-size: 1.5rem;
            }

            .header-icon {
                width: 50px;
                height: 50px;
                font-size: 1.25rem;
            }

            .image-gallery {
                grid-template-columns: 1fr 1fr;
            }

            .gallery-image {
                height: 100px;
            }

            .progress-step {
                padding: 0.75rem;
            }

            .step-icon {
                width: 32px;
                height: 32px;
                font-size: 0.875rem;
            }
        }

        /* Focus styles for accessibility */
        .btn:focus,
        .form-control:focus {
            outline: none;
            box-shadow: 0 0 0 3px rgba(52, 152, 219, 0.25);
        }

        /* Animation for page load */
        .details-card, .images-card, .summary-card, .notes-card, .status-update-card, .progress-card, .admin-actions-card {
            animation: fadeInUp 0.6s ease-out;
        }

        @@keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Loading states */
        .btn:disabled {
            opacity: 0.6;
            cursor: not-allowed;
            transform: none !important;
        }

        .btn:disabled:hover {
            transform: none !important;
        }

        /* Utility classes */
        .me-1 { margin-right: 0.25rem; }
        .me-2 { margin-right: 0.5rem; }
        .mb-3 { margin-bottom: 1rem; }
        .mb-4 { margin-bottom: 1.5rem; }
        .text-muted { color: var(--text-muted) !important; }
        .d-flex { display: flex; }
        .gap-2 { gap: 0.5rem; }
        .w-100 { width: 100%; }
    </style>
}